# Learn detailed information about the fields of an extension.yaml file in the docs:
# https://firebase.google.com/docs/extensions/reference/extension-yaml

name: cloudflare-appcheck-provider
version: 0.0.1 # Follow semver versioning
specVersion: v1beta # Version of the Firebase Extensions specification
# Friendly display name for your extension (~3-5 words)
displayName: Cloudflare Turnstile AppCheck Provider
# Brief description of the task your extension performs (~1 sentence)
description: >-
  The Cloudflare Turnstile AppCheck Provider for Firebase.
license: Apache-2.0 # https://spdx.org/licenses/
# Public URL for the source code of your extension
sourceUrl: https://github.com/change-me/to-your-repo
# Specify whether a paid-tier billing plan is required to use your extension.
# Learn more in the docs: https://firebase.google.com/docs/extensions/reference/extension-yaml#billing-required-field
billingRequired: true
tags: [cloudflare, appcheck, appcheck-provider]

author:
  authorName: Alexander Nohe
  url: https://nohe.dev

# In an `apis` field, list any Google APIs (like Cloud Translation, BigQuery, etc.)
# required for your extension to operate.
# Learn more in the docs:
# https://firebase.google.com/docs/extensions/reference/extension-yaml#apis-field

# In a `roles` field, list any IAM access roles required for your extension to operate.
# Learn more in the docs:
# https://firebase.google.com/docs/extensions/reference/extension-yaml#roles-field
roles:
  - role: iam.serviceAccounts.signBlob
    reason: We need the signBlob permission to mint App Check tokens on behalf of the project.

# In the `resources` field, list each of your extension's functions, including the trigger for each function.
# Learn more in the docs:
# https://firebase.google.com/docs/extensions/reference/extension-yaml#resources-field
resources:
  - name: tokenExchange
    type: firebaseextensions.v1beta.function
    description: >-
      Provides the token exchange service to exchange and validate Cloudflare Turnsitle tokens for AppCheck tokens.
    properties:
      # LOCATION is a user-configured parameter value specified by the user during installation.
      location: ${LOCATION}
      # httpsTrigger is used for an HTTP triggered function.
      httpsTrigger: {}
      runtime: "nodejs18"
# In the `params` field, set up your extension's user-configured parameters.
# Learn more in the docs:
# https://firebase.google.com/docs/extensions/reference/extension-yaml#params-field
params:
  - param: SECRETKEY
    label: Secret Key for site verification
    description: >-
      This is the secret key used for  For example, Hello world? or What's up, world?
    type: string
    default: Hello
    required: true
    immutable: false
  - param: APPID
    label: Firebase App Id
    description: >-
      This is the Firebase App Id.
    type: string
    required: true
    immutable: false
  - param: ttlMinutes
    label: TTL Minutes
    description: >-
      This is the length of the token validity in minutes.
    type: string
    required: true
    immutable: false
